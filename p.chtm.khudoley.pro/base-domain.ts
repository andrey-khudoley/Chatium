/**
 * Базовый URL воркспэйса (схема + хост).
 * Используется для webhook, партнёрских ссылок и т.д.
 * Измените при развёртывании на другой домен.
 */
export const BASE_DOMAIN = 'https://srv.aley.pro'
