// @shared
/**
 * Реестр путей проекта chatium-price.
 *
 * Проект одностраничный: единственный роут — корневой '/'. Межроутовых ссылок
 * по URL нет. Если понадобятся ссылки на роуты — использовать канонические
 * хелперы платформы (withProjectRoot / route.url(), см. 002-routing.md), а не
 * хардкод URL и не самодельные генераторы.
 */

// PROJECT_ROOT — путь от корня воркспэйса до проекта (без ведущего '/').
export const PROJECT_ROOT = 'p/units/aley/web/chatium-price'

// Пути роутов от корня проекта (справочно; хардкод URL в коде не допускается).
export const ROUTES = {
  index: '/'
} as const
