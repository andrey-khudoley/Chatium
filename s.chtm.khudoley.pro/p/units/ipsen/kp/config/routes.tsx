// @shared
/**
 * Реестр путей проекта.
 *
 * Сайт одностраничный: есть корневой роут '/' и публичный shared-route приёма
 * ответов. Межроутовых ссылок по URL в проекте нет — форма вызывает роут через
 * `apiCreateAnswerRoute.run(ctx, …)` по импорту модуля (см. `007-vue.md`), а не
 * по собранному URL, поэтому хелперы генерации ссылок здесь не нужны.
 *
 * ВАЖНО: не заводить локальный `getFullUrl`/`withProjectRoot` с расходящейся
 * семантикой — если понадобятся ссылки на роуты, использовать канонические
 * хелперы платформы (`002-routing.md`), а не самодельные.
 */

// PROJECT_ROOT — путь от корня воркспэйса до проекта (без ведущего '/').
export const PROJECT_ROOT = 'p/units/ipsen/kp'

// Пути роутов от корня проекта (справочно; хардкод URL в коде не допускается).
export const ROUTES = {
  index: '/',
  createAnswer: '/api/answers/create',
  allAnswers: '/api/answers/all'
} as const
